name: Telegram Bot Test

on:
  workflow_dispatch:  # lets you run this manually from GitHub Actions tab

jobs:
  telegram-test:
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram test message
        run: |
          TOKEN="8266904564:AAHaPMqWZKQEq-cXi-NBuOGFB8J4DpK4s9Y"
          CHAT_ID="6520801941"
          MESSAGE="✅ GitHub Action test: Bot is working!"

          curl -s -X POST "https://api.telegram.org/bot${TOKEN}/sendMessage" \
            -d chat_id="${CHAT_ID}" \
            -d text="${MESSAGE}"
